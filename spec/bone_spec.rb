# frozen_string_literal: true

RSpec.describe Bone do
  it "has a version number" do
    expect(Bone::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
